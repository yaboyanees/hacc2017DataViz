<div id="drill-down" style="min-width: 310px; height: 400px; margin: 0 auto"></div>
<div style="background:black; text-align:center">
<button id="by-location" class="mui-btn mui-btn--raised mui-btn--primary mui-btn--small">By Location</button>
<button id="by-priority" class="mui-btn mui-btn--raised mui-btn--primary mui-btn--small">By Strategic Priority</button>
<button id="by-results" class="mui-btn mui-btn--raised mui-btn--primary mui-btn--small">By Strategic Results</button>
</div>
<script>

var priority = true;
// Create the chart
drillDownChart = Highcharts.chart('drill-down', {
    chart: {
        type: 'pie'
    },
    title: {
        text: 'Grant funding by Strategic Priority'
    },
    subtitle: {
        text: 'Click slices to drill down.'
    },
    plotOptions: {
        series: {
            dataLabels: {
                enabled: true,
                format: '{point.name}<br/> ${point.y:,.0f}'
            }
        }
    },
	exporting: { buttons: { contextButton: {
		menuItems: [{
		textKey: 'printChart',
		onclick: function () {
			this.print();
		}
	}, {
		separator: true
	}, {
		textKey: 'downloadPNG',
		onclick: function () {
			this.exportChart();
		}
	}, {
		textKey: 'downloadJPEG',
		onclick: function () {
			this.exportChart({
				type: 'image/jpeg'
			});
		}
	}, {
		textKey: 'downloadSVG',
		onclick: function () {
			this.exportChart({
				type: 'image/svg+xml'
			});
		}
	}]
}}},

    tooltip: {
        headerFormat: '<span style="font-size:11px">{series.name}</span><br>',
        pointFormat: '<span style="color:{point.color}">{point.name}</span>: <b>${point.y:,.0f}<br/>'
    },
    series: [{
        name: 'Strategic Priorities',
        colorByPoint: true,
        data: [
		<% index = 0
	while index < @uPriorities.length - 1 %>
		{
			name: '<%= raw @uPriorities[index] %>',
			y: <%= raw @sumPriorities[index] %>,
			drilldown: '<%= raw @uPriorities[index] %>'
	    },
		<% index = index + 1 %>
	<% end %>
	{
        name: '<%= raw @uPriorities[index] %>',
        y: <%= raw @sumPriorities[index] %>,
		drilldown: '<%= raw @uPriorities[index] %>'
    }
		]
    }],
    drilldown: {
        series: [
		<% index = 0
	while index < @uPriorities.length - 1%>
		{
			name: '<%= raw @uPriorities[index] %>',
			id: '<%= raw @uPriorities[index] %>',
			data: [
				<% innerIndex = 0
				while innerIndex < @amountPerYearByPriority[index].length - 1 %>
					['<%= raw @uYears[innerIndex] %>', <%= raw @amountPerYearByPriority[index][innerIndex] %>],
					<% innerIndex = innerIndex + 1 %>
				<% end %>
				['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByPriority[index][innerIndex] %>]
			]
	    },
		<% index = index + 1 %>
	<% end %>
		{
			name: '<%= raw @uPriorities[index] %>',
			id: '<%= raw @uPriorities[index] %>',
			data: [
				<% innerIndex = 0
				while innerIndex < @amountPerYearByPriority[index].length - 1 %>
					['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByPriority[index][innerIndex] %>],
					<% innerIndex = innerIndex + 1 %>
				<% end %>
				['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByPriority[index][innerIndex] %>]
			]
	    }
		]
    }
});

$('#by-priority').click(function (e) {
	drillDownChart.destroy();
    drillDownChart2 = new Highcharts.chart('drill-down', {
    chart: {
        type: 'pie'
    },
    title: {
        text: 'Grant funding by Strategic Priority'
    },
    subtitle: {
        text: 'Click slices to drill down.'
    },
	exporting: { buttons: { contextButton: {
		menuItems: [{
		textKey: 'printChart',
		onclick: function () {
			this.print();
		}
	}, {
		separator: true
	}, {
		textKey: 'downloadPNG',
		onclick: function () {
			this.exportChart();
		}
	}, {
		textKey: 'downloadJPEG',
		onclick: function () {
			this.exportChart({
				type: 'image/jpeg'
			});
		}
	}, {
		textKey: 'downloadSVG',
		onclick: function () {
			this.exportChart({
				type: 'image/svg+xml'
			});
		}
	}]
}}},
    
    plotOptions: {
        series: {
            dataLabels: {
                enabled: true,
                format: '{point.name}<br/> ${point.y:,.0f}'
            }
        }
    },

    tooltip: {
        headerFormat: '<span style="font-size:11px">{series.name}</span><br>',
        pointFormat: '<span style="color:{point.color}">{point.name}</span>: <b>${point.y:,.0f}<br/>'
	},
    series: [{
        name: 'Strategic Priorities',
        colorByPoint: true,
        data: [
		<% index = 0
	while index < @uPriorities.length - 1 %>
		{
			name: '<%= raw @uPriorities[index] %>',
			y: <%= raw @sumPriorities[index] %>,
			drilldown: '<%= raw @uPriorities[index] %>'
	    },
		<% index = index + 1 %>
	<% end %>
	{
        name: '<%= raw @uPriorities[index] %>',
        y: <%= raw @sumPriorities[index] %>,
		drilldown: '<%= raw @uPriorities[index] %>'
    }
		]
    }],
    drilldown: {
        series: [
		<% index = 0
	while index < @uPriorities.length - 1%>
		{
			name: '<%= raw @uPriorities[index] %>',
			id: '<%= raw @uPriorities[index] %>',
			data: [
				<% innerIndex = 0
				while innerIndex < @amountPerYearByPriority[index].length - 1 %>
					['<%= raw @uYears[innerIndex] %>', <%= raw @amountPerYearByPriority[index][innerIndex] %>],
					<% innerIndex = innerIndex + 1 %>
				<% end %>
				['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByPriority[index][innerIndex] %>]
			]
	    },
		<% index = index + 1 %>
	<% end %>
		{
			name: '<%= raw @uPriorities[index] %>',
			id: '<%= raw @uPriorities[index] %>',
			data: [
				<% innerIndex = 0
				while innerIndex < @amountPerYearByPriority[index].length - 1 %>
					['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByPriority[index][innerIndex] %>],
					<% innerIndex = innerIndex + 1 %>
				<% end %>
				['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByPriority[index][innerIndex] %>]
			]
	    }
		]
    }
	
});
	drillDownChart = drillDownChart2;
});


$('#by-location').click(function (e) {
	drillDownChart.destroy();
var drillDownChart2 = new Highcharts.chart('drill-down', {
    chart: {
        type: 'pie'
    },
    title: {
        text: 'Grant funding by Location'
    },
    subtitle: {
        text: 'Click slices to drill down.'
    },
    plotOptions: {
        series: {
            dataLabels: {
                enabled: true,
                format: '{point.name}<br/> ${point.y:,.0f}'
            }
        }
    },
	exporting: { buttons: { contextButton: {
		menuItems: [{
		textKey: 'printChart',
		onclick: function () {
			this.print();
		}
	}, {
		separator: true
	}, {
		textKey: 'downloadPNG',
		onclick: function () {
			this.exportChart();
		}
	}, {
		textKey: 'downloadJPEG',
		onclick: function () {
			this.exportChart({
				type: 'image/jpeg'
			});
		}
	}, {
		textKey: 'downloadSVG',
		onclick: function () {
			this.exportChart({
				type: 'image/svg+xml'
			});
		}
	}]
}}},
    tooltip: {
        headerFormat: '<span style="font-size:11px">{series.name}</span><br>',
        pointFormat: '<span style="color:{point.color}">{point.name}</span>: <b>${point.y:,.0f}<br/>'
    },
    series: [{
        name: 'Locations',
        colorByPoint: true,
        data: [
		<% index = 0
	while index < @uLocations.length - 1 %>
		{
			name: '<%= raw @uLocations[index] %>',
			y: <%= raw @sumLocations[index] %>,
			drilldown: '<%= raw @uLocations[index] %>'
	    },
		<% index = index + 1 %>
	<% end %>
	{
        name: '<%= raw @uLocations[index] %>',
        y: <%= raw @sumLocations[index] %>,
		drilldown: '<%= raw @uLocations[index] %>'
    }
		]
    }],
    drilldown: {
        series: [
		<% index = 0
	while index < @uLocations.length - 1%>
		{
			name: '<%= raw @uLocations[index] %>',
			id: '<%= raw @uLocations[index] %>',
			data: [
				<% innerIndex = 0
				while innerIndex < @amountPerYearByLocation[index].length - 1 %>
					['<%= raw @uYears[innerIndex] %>', <%= raw @amountPerYearByLocation[index][innerIndex] %>],
					<% innerIndex = innerIndex + 1 %>
				<% end %>
				['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByLocation[index][innerIndex] %>]
			]
	    },
		<% index = index + 1 %>
	<% end %>
		{
			name: '<%= raw @uLocations[index] %>',
			id: '<%= raw @uLocations[index] %>',
			data: [
				<% innerIndex = 0
				while innerIndex < @amountPerYearByLocation[index].length - 1 %>
					['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByLocation[index][innerIndex] %>],
					<% innerIndex = innerIndex + 1 %>
				<% end %>
				['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByLocation[index][innerIndex] %>]
			]
	    }
		]
    }
	
});
	drillDownChart = drillDownChart2;
});


$('#by-results').click(function (e) {
	drillDownChart.destroy();
var drillDownChart2 = new Highcharts.chart('drill-down', {
    chart: {
        type: 'pie'
    },
    title: {
        text: 'Grant funding by Results'
    },
    subtitle: {
        text: 'Click slices to drill down.'
    },
    plotOptions: {
        series: {
            dataLabels: {
                enabled: true,
                format: '{point.name}<br/> ${point.y:,.0f}'
            }
        }
    },
	exporting: { buttons: { contextButton: {
		menuItems: [{
		textKey: 'printChart',
		onclick: function () {
			this.print();
		}
	}, {
		separator: true
	}, {
		textKey: 'downloadPNG',
		onclick: function () {
			this.exportChart();
		}
	}, {
		textKey: 'downloadJPEG',
		onclick: function () {
			this.exportChart({
				type: 'image/jpeg'
			});
		}
	}, {
		textKey: 'downloadSVG',
		onclick: function () {
			this.exportChart({
				type: 'image/svg+xml'
			});
		}
	}]
}}},
    tooltip: {
        headerFormat: '<span style="font-size:11px">{series.name}</span><br>',
        pointFormat: '<span style="color:{point.color}">{point.name}</span>: <b>${point.y:,.0f}<br/>'
    },
    series: [{
        name: 'Strategic Results',
        colorByPoint: true,
        data: [
		<% index = 0
	while index < @uResults.length - 1 %>
		{
			name: '<%= raw @uResults[index] %>',
			y: <%= raw @sumResults[index] %>,
			drilldown: '<%= raw @uResults[index] %>'
	    },
		<% index = index + 1 %>
	<% end %>
	{
        name: '<%= raw @uResults[index] %>',
        y: <%= raw @sumResults[index] %>,
		drilldown: '<%= raw @uResults[index] %>'
    }
		]
    }],
    drilldown: {
        series: [
		<% index = 0
	while index < @uResults.length - 1%>
		{
			name: '<%= raw @uResults[index] %>',
			id: '<%= raw @uResults[index] %>',
			data: [
				<% innerIndex = 0
				while innerIndex < @amountPerYearByResults[index].length - 1 %>
					['<%= raw @uYears[innerIndex] %>', <%= raw @amountPerYearByResults[index][innerIndex] %>],
					<% innerIndex = innerIndex + 1 %>
				<% end %>
				['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByResults[index][innerIndex] %>]
			]
	    },
		<% index = index + 1 %>
	<% end %>
		{
			name: '<%= raw @uResults[index] %>',
			id: '<%= raw @uResults[index] %>',
			data: [
				<% innerIndex = 0
				while innerIndex < @amountPerYearByResults[index].length - 1 %>
					['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByResults[index][innerIndex] %>],
					<% innerIndex = innerIndex + 1 %>
				<% end %>
				['<%= raw @uYears[innerIndex] %>', <%= @amountPerYearByResults[index][innerIndex] %>]
			]
	    }
		]
    }
	
});
	drillDownChart = drillDownChart2;
});

</script>